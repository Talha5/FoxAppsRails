<%= form_for(@jurisdiction, :html => { :class => 'form-horizontal' } ) do |f| %>
  <% if @jurisdiction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jurisdiction.errors.count, "error") %> prohibited this jurisdiction from being saved:</h2>

      <ul>
      <% @jurisdiction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <fieldset>
    <legend>Jusisdiction</legend>
    <div class="form-group">
      <%= f.label :name, :class => 'col-lg-2 control-label' %>
      <div class="col-xs-4">
        <%= f.text_field :name, :class => 'form-control' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :short_name, :class => 'col-lg-2 control-label' %>
      <div class="col-xs-2">
        <%= f.text_field :short_name, :class => 'form-control' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :zip, :class => 'col-lg-2 control-label' %>
      <div class="col-xs-3">
        <%= f.text_field :zip, :class => 'form-control' %>
      </div>
    </div>

    <div class="form-group">
      <div class="col-lg-10 col-lg-offset-2">
        <%= f.submit 'Submit', :class => "btn btn-primary" %>
      </div>
    </div>
  </fieldset>

<% end %>
